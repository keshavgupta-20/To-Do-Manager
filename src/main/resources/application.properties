spring.application.name=Todo-Manager
server.port = 9090
#max file size
spring.servlet.multipart.max-file-size = 20MB

#max Request size
spring.servlet.multipart.max-request-size = 20MB

#url

spring.datasource.url =  jdbc:mysql://localhost:3306/boot_course
spring.datasource.username = root
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver
spring.datasource.password = Keshav@123
spring.jpa.hibernate.ddl-auto = update

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
# Show SQL queries
spring.jpa.show-sql=true

# Format SQL for readability
spring.jpa.properties.hibernate.format_sql=true

# Optional: Show SQL with comments for extra context (useful for debugging)
spring.jpa.properties.hibernate.use_sql_comments=true
